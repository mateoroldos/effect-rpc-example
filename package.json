{
  "name": "effect-template",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "./scripts/prepare-effect.sh && effect-tsgo patch",
    "dev": "bun --env-file=.env turbo dev",
    "dev:full": "bun run telemetry:up && OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318 bun --env-file=.env turbo dev",
    "db:up": "docker compose up -d --wait postgres",
    "db:down": "docker compose down --remove-orphans",
    "db:destroy": "docker compose down --volumes --remove-orphans",
    "db:generate": "bun run --cwd packages/database db:generate",
    "db:migrate": "bun --env-file=.env run --cwd apps/server db:migrate",
    "telemetry:up": "./scripts/start-maple.sh",
    "infra:deploy": "alchemy deploy --yes",
    "infra:deploy:prod": "alchemy deploy --yes --stage prod",
    "infra:destroy": "alchemy destroy --yes",
    "check-types": "turbo check-types && tsc -p tsconfig.alchemy.json",
    "test": "vitest run",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "knip": "knip"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "alchemy": "2.0.0-beta.65",
    "effect": "catalog:",
    "@effect/language-service": "^0.87.1",
    "@effect/tsgo": "^0.24.3",
    "@effect/vitest": "4.0.0-beta.102",
    "@types/bun": "catalog:",
    "knip": "^6.29.0",
    "portless": "^0.15.5",
    "turbo": "^2.8.12",
    "typescript": "^7.0.2",
    "vitest": "^4.0.18",
    "ultracite": "^7.8.1"
  },
  "packageManager": "bun@1.3.14",
  "catalog": {
    "@effect/platform-node": "4.0.0-beta.102",
    "@types/bun": "^1.3.14",
    "effect": "^4.0.0-beta.102"
  }
}
